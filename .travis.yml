os: linux
language: minimal
services:
  - docker

before-install:
  docker build -t kam1runetz/ci .

jobs:
  include:
    - name: "memory_checks"
      script:
        - docker run -v `pwd`:`pwd` -w `pwd` --cap-add=SYS_PTRACE -t kam1runetz/ci scripts/checks.sh

    - name: "static_analyze"
      script:
        - docker run -v `pwd`:`pwd` -w `pwd` -t kam1runetz/ci scripts/static_analyze.sh

    - name: "linters"
      script:
        - docker run -v `pwd`:`pwd` -w `pwd` -t kam1runetz/ci scripts/linters.sh

    - name: "unit_tests"
      script:
        - docker run -v `pwd`:`pwd` -w `pwd` -t kam1runetz/ci scripts/tests.sh

    - name: "coverage"
      script:
        - docker run -v `pwd`:`pwd` -w `pwd` -t  kam1runetz/ci scripts/coverage.sh

after_succsess:
  - docker run -v `pwd`:`pwd` -w `pwd` -t kam1runetz/ci scripts/push_report.sh

notifications:
  email: false

branches:
  except:
    - master
